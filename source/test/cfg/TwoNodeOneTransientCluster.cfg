#
# Copyright 2014 Formation Data Systems, Inc.
#
# FDS configuration for starting a two-node cluster.

[user]
user_name       = root
password        = passwd

# The 'node' section defines a nodes parameters. The section must be prefixed
# with 'node' but is also used as a unique ID for the node.
#
# Node section names match the names that will be auto-generated by OM.
#
[Node-1]
# Denotes this node will run an OM
om              = true

# IP of the node
ip              = localhost

# Fds root directory to use
fds_root        = /fds/Node-1

# Base port for this node. Not needed if we don't run all nodes one physical machine
fds_port        = 7000

[Node-2]
enable          = true
transient       = true
ip              = localhost
fds_root        = /fds/Node-2
fds_port        = 7100


# This is where we put our AMs
[sh1]
blk             = false
fds_node        = Node-1
