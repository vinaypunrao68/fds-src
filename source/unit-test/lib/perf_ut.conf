fds:
{
    perf_ut:
    {
        logfile="perf_ut.log"

        enable_graphite=false

        log_severity="trace"

        perf:
        {
            enable=true
            #event_types="0-100"
            #match=".*perf-ut-.*"
        }
    }
}
