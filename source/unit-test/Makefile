topdir         := ..
include $(topdir)/Makefile.cpus
speedup        := -j $(parallel_build_count)
user_build_dir :=      \
    lib                \
    fds-probe          \
    orch-mgr           \
    persistent-layer   \
    umod-lib           \
    util               \
    migrations         \
    stor-mgr           \
    data-mgr

include $(topdir)/Makefile.incl
